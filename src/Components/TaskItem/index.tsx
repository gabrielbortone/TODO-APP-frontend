import { TodoItemProps } from "../../CommonInterfaces/TodoItemProps";
import { TaskContainer } from "./styles";

/*{ title, description, priority, dueDate, categoryId, categoryName, finishDate }: TodoItemProps*/

export function TaskItem() {

    return (
      <TaskContainer>
        
      </TaskContainer>
    )
  }